namespace nucs.Automation.Controllers {
    public enum MouseButton {
        Left,
        Right,
        Middle,
        Scroll,
    }

    public enum MouseDirection {
        Up,
        Down
    }
}